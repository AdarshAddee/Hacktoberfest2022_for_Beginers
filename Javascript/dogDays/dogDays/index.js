function myFunction() {
    let dogName = prompt('What is your dog\'s name?');

    let dogAge = prompt('How old is ' + dogName + ' ?');

    let humanAge = (dogAge - 2) * 4 + 21;
         alert(dogName + ' is ' + humanAge + ' in human years!')
  }
  
  // Github username: Tahree22. 
  // Aim: This repo demonstrates a simple use of variables in javascript and the box model in CSS. 
  // Date: October 24, 2022.
// Suraj-Bhandarkar-S 

let limit = "..........".length;

for(let i = ".".length;i <= (limit * limit);i++){
    console.log(i);
}

// to run the file in vscode: node '.\print 1 to 100 without numbers.js'
